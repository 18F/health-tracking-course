{% capture id %}
{%- include uuid -%}
{% endcapture %}
(<span id="count-{{ id }}" onclick="countdownTimer({{ include.minutes }}, 'count-{{ id }}')" style="color: #0645AD;">{{ include.minutes }}m timer</span>)