{% assign id = include.id | append: include.minutes | append: "m" %}{% assign minutes = include.minutes %}
(<span id="{{ id }}" onclick="countdownTimer({{ minutes }}, '{{ id }}')" style="color: #0645AD;">{{minutes}}m timer</span>)